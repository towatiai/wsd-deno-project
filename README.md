# CS-C3170: WSD Course Project

## Initialization
Add .env file and fill database url. See .env.sample for template. To create necessary databases, run `deno run -A init.js`.

## Running

Start application with `deno run -A app.js`.